import os

# List names of all files and folders in current directory
entries = os.listdir()
for entry in entries:
    print(entry) 
    # prints the name of the files in the current opened desktop


